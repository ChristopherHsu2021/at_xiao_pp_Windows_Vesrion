# AT小PP 未预制语音文案清单

> 生成时间：2026-09-03
> 审计基线：源码 `app/` + 配置 `data/character_config.json` + 预制目录 `data/voice/Generated Voice Media/`（共 89 个 `.wav`）

## 机制说明

`app/core/voice.py` 的 `VoiceEngine.speak()` 优先调用 `voice_media.find_prepared(text)` 播放**预生成音频**；若没有，则回退到 Windows SAPI5 / 本地 GPT-SoVITS 实时合成。

预制文件命名规则（见 `voice_media.py`）：

```
data/voice/Generated Voice Media/{sha1(text.strip())}.wav
```

即「播报文案」经 `sha1` 得到文件名。**句尾「了/呢」会被 `apply_ni()` 转为「捏」后再算 hash**，本清单已按该规则比对。

## 统计

| 类别 | 已预制 | 未预制 |
|---|---|---|
| 代码固定文案（硬编码 / `tr(...)` 常量） | 16 | 11 处（去重 7 条） |
| 配置台词（`character_config.json` 的 `lines`/`bootLines` 等） | 63 | 8 处（去重 4 条） |
| 代码动态 / 数据驱动调用（变量、条件、`pick_line` 等） | — | 20 处（运行时文本来自配置，已在上方覆盖） |

## 一、代码固定文案未预制（7 条独特文案，11 处引用）

| # | 文案 | 出现位置 |
|---|---|---|
| 1 | 无法连接音乐服务，请检查网络后重试 | `app/ui/music_player.py:2574`、`2591` |
| 2 | 该歌曲受版权保护，仅可在酷我音乐手机版播放 | `app/ui/music_player.py:2578`、`2597` |
| 3 | 播放出错 | `app/ui/music_player.py:2580` |
| 4 | 歌曲已存在 | `app/ui/music_player.py:2588`、`3906` |
| 5 | 下载出错 | `app/ui/music_player.py:2599` |
| 6 | 已添加到我喜欢 | `app/ui/music_player.py:2610`、`3073` |
| 7 | 已取消我喜欢 | `app/ui/music_player.py:3106` |

> 注：`music_player.py:2610` 为 `tr("已添加到我喜欢") if ... else tr("下载完成")` 条件分支；其中「下载完成」已预制，故未列入。

## 二、配置台词未预制（4 条独特文案，8 处引用）

| # | 文案 | 配置路径 |
|---|---|---|
| 1 | 嘻嘻～我来啦捏 | `basicSetting/bootLines`（启动台词之一，另两句已预制） |
| 2 | 嘻嘻，这个味道好浓烈捏 | `drinks/categoryList[0]/items[0][1][2][5][6]/lines`（5 个酒类饮品共用同一句，均未预制） |
| 3 | 嘻嘻，来听听歌放松捏 | `home/itemList[2]/lines`（居家场景·听歌） |
| 4 | 嘻嘻，出来度假放松捏 | `home/itemList[7]/lines`（居家场景·度假） |

## 三、补充说明

- **动态 / 数据驱动调用（20 处）**：如 `pet_window.py` 的 `say(line)`、`scene_window.py` 的 `pick_line(cfg.get("lines", []))`、`main.py` 的 `t["content"] + "，时间到捏"`、`config.character.system_func.get(..., "默认提示")` 等。这些文案运行时由配置/变量决定，其实际文本已在「配置台词」核查中覆盖，**未额外产生未预制项**。绝大多数系统提示（闹钟/计时/设置/待办）已预制。
- **已预制的台词主体**：角色状态台词（`statusConfig` 清醒/微醺/有点醉/醉醺醺）、启动台词（3 句中 2 句）、饮品台词（`drinks` 中其余条目）、各场景 `lines` 等共 63 条。
- **补录方式**：对上表任一条文案 `T`，调用
  `from app.core import voice_media; voice_media.generate_prepared(T)`
  即可生成对应 `wav` 到 `data/voice/Generated Voice Media/`（需配置好本地 GPT-SoVITS 或手工替换为录制文件，命名取 `sha1(T.strip()) + ".wav"`）。

---

## ✅ 更新记录（2026-09-03 下午）：11 条已全部预制补齐

用户按文件名录制了 11 个 wav（文件名即播报文案），已按 `sha1(文件名.trim())` 落盘至
`data/voice/Generated Voice Media/`（共 100 个 wav：89 + 11），并同步写入运行时
`%APPDATA%\AT小PP\voice\Generated Voice Media/`。其中 **4 处文案与旧代码/配置不一致**，已对齐：

| 旧文案（代码中） | 新文案（预制音频文件名） | 对齐位置 |
|---|---|---|
| 该歌曲受版权保护，仅可在酷我音乐手机版播放 | 该歌曲暂无版权 | `music_player.py` 两处 `say` |
| 播放出错 | 播放出错噜 | `music_player.py:2580` |
| 下载出错 | 下载出错噜 | `music_player.py:2599` |
| 嘻嘻～我来啦捏（bootLines[0]） | 嘻嘻，我来了捏 | `character_config.json` |

其余 7 条文件名与代码文案一致，无需改代码。校验结果：活动源码旧文案已清零、JSON 合法、
`py_compile` 通过、11/11 全部命中预制音频。重新打包后安装版即可播放这批预制语音。
